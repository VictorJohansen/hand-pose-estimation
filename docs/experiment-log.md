# Experiment log

Tracks the serious training runs for the hand-pose-estimation project.
Each run is reproducible from the canonical FreiHAND split
(`SPLIT_SEED=42`, `validation_fraction=0.1`) and stores artifacts under
parallel `models/<run>/`, `logs/<run>/`, and `artifacts/<run>/`
directories.

## How to add a new run

1. Train, e.g. `python -m src.training.train_baseline --model-id baseline-model-1`
   or `python -m src.training.train_improved`. Outputs
   land in `models/<name>/best.keras`, `logs/<name>/config.json`, and
   `logs/<name>/history.json`.
2. Compute MPKE on the canonical validation split:
   `python -m src.evaluation.evaluate_run <name>`. Writes
   `artifacts/<name>/evaluation.json`.
3. Refresh the comparison table:
   `python -m src.evaluation.comparison <run-a> <run-b> ...`.
4. Regenerate report figures:
   `python -m src.evaluation.report_figures`.

## Comparison

Generated by `python -m src.evaluation.comparison baseline-model-1 baseline-model-2 improved-model-1`:

| Run              | Model ID         | Repr.      | Params     | MPKE (px) | Median (px) | p90 (px) | p95 (px) | Max (px) |
| ---------------- | ---------------- | ---------- | ---------- | --------- | ----------- | -------- | -------- | -------- |
| baseline-model-1 | baseline-model-1 | coordinate | 137,962    | 18.10     | 17.22       | 26.32    | 30.06    | 75.99    |
| baseline-model-2 | baseline-model-2 | coordinate | 12,990,410 | 21.19     | 20.78       | 28.21    | 30.61    | 66.53    |
| improved-model-1 | improved-model-1 | heatmap    | 754,229    | 8.59      | 7.43        | 14.30    | 17.36    | 52.44    |

## Report figures

Generated by `python -m src.evaluation.report_figures`:

- `reports/figures/report_primary_results.{png,pdf}`: validation MPKE and
  parameter count comparison.
- `reports/figures/report_dataset_examples.{png,pdf}`: FreiHAND examples
  with ground-truth keypoints.
- `reports/figures/report_error_summary.{png,pdf}`: median, p75, p90,
  and p95 per-sample MPKE.
- `reports/figures/report_training_curves.{png,pdf}`: train/validation
  loss and MAE curves for each run.
- `reports/figures/report_prediction_comparison.{png,pdf}`: shared
  validation examples comparing all three models against ground truth.
